# Environment Variables Template - SAFE TO COMMIT
# Copy this file to .env and fill in your REAL values
# NEVER commit the actual .env file - it contains secrets!

# Flask Configuration (REQUIRED)
FLASK_SECRET_KEY=generate-a-random-32-character-secret-key-here
FLASK_ENV=development

# Google Cloud Platform (OPTIONAL - for AI features)
# Get these from Google Cloud Console
GCP_PROJECT_ID=replace-with-your-actual-project-id
GOOGLE_APPLICATION_CREDENTIALS=./path-to-your-service-account-key.json

# Database Configuration
DATABASE_URL=sqlite:///database.db

# Upload Configuration  
MAX_CONTENT_LENGTH=10485760
UPLOAD_FOLDER=temp_uploads

# SECURITY REMINDER:
# - The .env file (with real values) is automatically excluded from Git
# - Only commit this .env.example file (with fake/example values)
# - Never put real API keys in any file that goes to GitHub!
